"use client";
// STUB — replaced by Agent A
export function TopBar() {
  return (
    <div className="absolute top-0 inset-x-0 z-40 h-12 flex items-center px-4 bg-black/60 backdrop-blur border-b border-white/10 text-[12px] font-[family-name:var(--font-jetbrains)] tracking-wider uppercase text-[color:var(--nafas-ink3)]">
      NAFAS · MONITOR · Chantier (Agent A pending)
    </div>
  );
}
